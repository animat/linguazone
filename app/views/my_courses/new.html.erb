<h1>Create a new class</h1>
<br />
<div>
	<%= form_for @course, :url => {:action => "create"} do |f| %>
		<%= render :partial => 'form', :locals => {:form => f} %>
		<p align="center"><%= f.submit "Create this class" %></p>
	<% end %>
</div>
