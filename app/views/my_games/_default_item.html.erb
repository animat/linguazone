<% unless item.game.nil? %>
	<div class="game_listing" id="<%= dom_id(item) %>">
		<div class="game_details">
			<% unless item.game.activity.nil? %>
				<%= link_to image_tag('/games/'+item.game.activity.swf+'/display/icon-small.jpg', :alt => 'Play '+item.game.activity.name), play_path(item.id) %>
			<% end %>
			<p>
				<%= item.game.description %>
				<br />
				<span class="date">Last updated <%= format_short_date(item.game.updated_at) %></span>
			</p>
		</div>
	
		<div class="manage_game">
			<%= link_to 'Edit', :controller => "customize", :action => "edit", :id => item.game.id, :cmzr_type => "game" %>
			|
			<%= link_to 'Delete', my_game_path(item.game), :method => "DELETE" %>
		</div>
		<br class="clearFloat" />
	</div>
<% end %>