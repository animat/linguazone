<%= render :partial => "subscription/checkpoint" %>

<%= render :partial => "checkpoint" %>

<div class="progressContent">
	<% form_tag :controller => "schools", :action => "confirm_or_new" do |f| %>
		<div>
			<%= label :school, :name, "Your school's name:" %><br />
			<%= text_field_with_auto_complete :school, :name, {:size => 34} %>
			<%= submit_tag "Check the database" %>
		</div>
	<% end %>
</div>

<%= render :partial => "teachers/checkpoint" %>