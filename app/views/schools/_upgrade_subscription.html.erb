<br />
<p><%= session[:school].name %> has a <strong><%= session[:school].subscription.subscription_plan.name %> subscription</strong>. <strong><%= pluralize(session[:school].subscribers.length, "teacher") %></strong> are currently registered with a maximum allowed group of <strong><%= session[:school].subscription.subscription_plan.max_teachers %></strong>.
</p>
<p>To upgrade <%= session[:school].name %>'s account for $<%= String(@upgrade_plan.cost - session[:school].subscription.subscription_plan.cost) %> and create your account, enter your school's pin number below.</p>

<div class="floatLeft">
	<%= form_tag :controller => "subscription", :action => "upgrade_new" do %>
	<p>
		<%= label_tag :pin, "Enter your school's 5-digit pin number:" %>
		<%= text_field_tag :pin %>
		<%= hidden_field_tag :subscription_plan_id, @upgrade_plan.id %>
		<%= submit_tag "Proceed" %><br />
		<span class="input_hint">(Don't know your school's pin number? No problem. <%= link_to "Contact us", contact_path %>, or ask a colleague who uses LinguaZone.)</span>
	</p>
	<% end %>
</div>
<br class="clearFloat" />